<header class="nav">
  <div class="brand">
    zoption
    <span class="brand-sub">NIFTY offset scalper</span>
  </div>
  <nav>
    <a href="/"          class="<%= page === 'dashboard' ? 'on' : '' %>">Dashboard</a>
    <a href="/trades"    class="<%= page === 'trades'    ? 'on' : '' %>">Trades</a>
    <a href="/events"    class="<%= page === 'events'    ? 'on' : '' %>">Audit</a>
    <a href="/settings"  class="<%= page === 'settings'  ? 'on' : '' %>">Settings</a>
    <a href="/brokers"   class="<%= page === 'brokers'   ? 'on' : '' %>">Broker</a>
  </nav>
  <form method="post" action="/logout" class="logout">
    <button type="submit" class="link">Sign out</button>
  </form>
</header>
