<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= typeof title !== 'undefined' ? title + ' · ' : '' %>Premium Range Trader</title>
  <link rel="stylesheet" href="/static/css/app.css" />
  <link rel="icon" href="data:," />
  <!-- Loaded synchronously in <head>: every page's inline script calls into it
       during parsing, so it must already be defined by then. -->
  <script src="/static/js/common.js"></script>
</head>
<body>
